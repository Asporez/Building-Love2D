local love = require( 'love' )

local game = {
    state = {
        intro = true,
        menu = false,
        running = false,
        ended = false
    }
}

function love.load()
    
end

function love.update(dt)
    
end

function love.draw()
    
end